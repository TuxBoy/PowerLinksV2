{% extends 'base.html.twig' %}

{% block title 'Mon profil' %}

{% block body %}

    <h1>Profil de {{ user.username }}</h1>

    <div class="card">
        <div class="card-header">
            Informations
        </div>
        <div class="card-body">
           Vous avez ajouté <strong>{{ totalLinks }}</strong> <a href="{{ path('user.links') }}" title="Voir tous mes liens">liens</a>
            {% if totalLinks > 1 %}
                <h3><i class="far fa-grin-beam"></i></h3>
            {% else %}
                <h3><i class="far fa-frown"></i></h3>
            {% endif %}
        </div>
    </div>

{% endblock %}

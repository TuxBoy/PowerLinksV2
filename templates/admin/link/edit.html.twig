{% extends 'admin/base.html.twig' %}

{% block title 'Liens' %}

{% block body %}
    <h1>Editer le lien {{ link.name }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}
{% endblock %}
